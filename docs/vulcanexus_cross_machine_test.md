# Vulcanexus Cross-Machine Trajectory Test

The full step-by-step guide now lives in:

- `scripts/vulcanexus/README.md`

Open it here:

- `/home/vvijaykumar/vilma-agent/scripts/vulcanexus/README.md`

That README includes:

- how to find the correct IP on each machine
- why `<DISCOVERY_SERVER_IP>` is not a literal shell value
- exact commands for same-network direct publish/subscribe
- exact commands for Fast DDS Discovery Server
- exact commands for different-network DDS Router
- troubleshooting steps and validation commands
